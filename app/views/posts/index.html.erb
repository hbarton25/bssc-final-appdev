<div class="page-header">
  <h1 class="centered">
    Posts </h1>
  <a href="/posts/new" class="btn btn-block btn-success float-left">New Post</a>
  <a href="/newsfeed/<%= current_user.trip_id %>" class="btn btn-block btn-primary">Back to Newsfeed</a>
</div>

<div class="row">
  <div class="col-md-12">
    <table class="table table-striped table-hover">
      <tr>
        <th>Date</th>
        <th>Name</th>
        <th>Title</th>
        <th>Body</th>
      </tr>

      <% @posts.each do |post| %>
      <tr>
        <td><%= post.created_at %></td>
        <td><%= post.user.first_name  %></td>
        <td><%= post.title %></td>
        <td><%= post.body %></td>
        <td>
          <a href="/posts/<%= post.id %>" class="btn btn-primary">Show</a>
          <% if current_user == post.user %>
          <a href="/posts/<%= post.id %>/edit" class="btn btn-warning">Edit</a>
          <a href="/delete_post/<%= post.id %>" class="btn btn-danger" rel="nofollow">Delete</a>
          <% else %>
          <% end %>
        </td>
      </tr>
      <% end %>
    </table>
  </div>
</div>
